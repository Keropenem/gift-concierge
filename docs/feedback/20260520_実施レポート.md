# サービス改善 実施レポート（2026-05-20）— Google Sheets 改訂用

対象: 共有スプレッドシート（`20260520サービス改善.xlsx` ベース）
対応者: 笠原（コード側）
反映ヘッドコミット: `91e4180`（+ 絵文字拡大・ピッカー即遷移修正）

---

## 使い方

各行ごとに **L列（実施ステータス）** に入れる値と、必要なら **G列（備考）** に追記するコメントを書いてあります。コピペで反映してください。

ステータスの語彙:
- `完了` … 今回コード対応済（既に完了だったものも含む）
- `保留（プロンプト）` … Gemini APIのプロンプト改修が必要 → 辻村さん側のチューニング待ち

---

## 行2 — #1（マイページ：贈った相手ダブり）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**: （変更なし。以前完了済み）

---

## 行3 — #2（提案：URLが間違っている）

- **L列 実施ステータス**: `保留（プロンプト）`
- **G列 備考に追記**:
```
プロンプト側で「Web検索結果から実在URLを厳格に採用」を強化する領域。コード側はノータッチ。
```

---

## 行4 — #3（提案：URLがリンクと判別できない → 水色+下線）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**: （変更なし。以前完了済み）

---

## 行5 — #4（フィードバック：提案結果のFBボタン）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**:
```
提案メッセージ下に3段階リアクション（😊ニコニコ／😐普通／😞ダメ）＋任意コメント欄を実装。コメントは書きたい人だけ。feedback テーブル新設、同一メッセージは上書き保存。評価ボタンは絵文字を大きく＋横にラベル併記して読みやすく。
```

---

## 行6 — #5（提案：2度目以降の自分プロフィールブロックが蛇足）

- **L列 実施ステータス**: `保留（プロンプト）`
- **G列 備考に追記**:
```
受け手データ・送り手プロフィールはコード側で渡しているが、「既に把握済みなのでStep冒頭で改めて触れない」挙動をLLMに守らせるのはプロンプト調整領域。
```

---

## 行7 — #6（提案：URLが同タブ遷移 → 新タブ）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**: （変更なし。以前完了済み）

---

## 行8 — #7（提案：2・3個目のURLが誤り）

- **L列 実施ステータス**: `保留（プロンプト）`
- **G列 備考に追記**:
```
#2 と同系統。プロンプト改修担当。
```

---

## 行9 — #8（提案：10万円予算でWeb購入不可な商品）

- **L列 実施ステータス**: `保留（プロンプト）`
- **G列 備考に追記**:
```
「実在しWebで購入可能なURL必須」をプロンプトで強化する範疇。コード側でDB絞り込みを入れる前段階。
```

---

## 行10 — #9（SP：改行しようとEnterで送信される）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**:
```
SP（タッチデバイス）ではEnter＝改行に変更。送信は送信ボタンタップに統一。PC側は従来通り（Enter送信 / Shift+Enterで改行）。
```

---

## 行11 — #10（SP：書き込み中スクロールできず長文確認不能）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**:
```
textareaを overflow-y-auto + max-h-[200px] に変更。長文入力時も内部スクロールで全行確認可能。
```

---

## 行12 — #11（対話：全入力が対話のみ→フォーム選択式併用）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**:
```
フォーム式モード /chat/form を新設（A/Bテスト用に対話モードと両立）。Step 1-3 を一括フォーム入力→既存APIに整形メッセージで投入。ログイン済みなら過去のプロフィール・受け手情報を初期値に自動反映。トップに「まとめてフォームで入力する」リンク追加。
```

---

## 行13 — #12（提案：ビジュアル参考写真がない）

- **L列 実施ステータス**: `保留（プロンプト）`
- **G列 備考に追記**:
```
LLM出力に画像URLを含めさせる、または別の画像APIで補完する設計が必要。プロンプト＋外部API判断領域。
```

---

## 行14 — #13（SP：パスワードリセット不可）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**:
```
3画面構成で実装: /auth/forgot-password（メール送信）→ /auth/callback（セッション確立）→ /auth/reset-password（新パスワード設定）。ログイン画面に「パスワードを忘れた方」リンク追加。Supabase Auth resetPasswordForEmail 使用。※ Supabaseのメールテンプレートで Reset Password のリダイレクトURLを本番ドメインに合わせる作業が別途必要。
```

---

## 行15 — #14（対話：白紙UIの取っ掛かり → 入口ボタン）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**:
```
トップに2つの入口ボタンを追加: 🎁「ギフトについて相談する」/ ✨「自分が欲しい物を相談する」。クリックで /chat?intent=gift|self に遷移→受け手ピッカー＋目的別ヒントを表示。受け手をクリックすると「〇〇への贈り物を考えています。」を自動送信して即チャット開始（過去登録した相手情報も自動反映）。
```

---

## 行16 — #15（対話：中間提案・条件絞りプロセス／A/Bテスト希望）

- **L列 実施ステータス**: `保留（プロンプト）`
- **G列 備考に追記**:
```
プロンプト側で「方向性の中間提案を出してユーザー確認を取る」フローを組む必要があるため、辻村さん側のチューニング領域。
```

---

## 行17 — #16（対話：工数の見える化／ステップ数）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**:
```
チャットヘッダー下に Step 1-7 プログレスバーを追加（1.あなた / 2.相手 / 3.関係性 / 4.分析 / 5.物語 / 6.提案 / 7.演出）。往復数＋「Step 6」「商品名」等のキーワード検出で現在位置を推定し点灯。
```

---

## 行18 — #17（提案：履歴クリック時に黒いシステムプロンプトが見える）

- **L列 実施ステータス**: `完了`
- **G列 備考に追記**:
```
保存形式を改修。新規セッション作成時に純粋なユーザー入力を profile_input._originalMessage に分離保存し、復元APIで messages[0] のテキストをこれで差し替える。旧セッション向けに「【...】」ブロック除去の正規表現フォールバックも実装。今後の履歴再開では黒バブルが見えない。
```

---

## 行19 — #18（提案：最終提案が1つで外す → 3方向性で分岐）

- **L列 実施ステータス**: `保留（プロンプト）`
- **G列 備考に追記**:
```
プロンプト側で「3つの方向性を提示してユーザー選択を待つ」フローを組む範疇。辻村さん側のチューニング領域。
```

---

# 会議由来の追加実装（Excel 外・追記が必要なら新規行で）

Excelにない要望だが 5/20 会議で出たもの:

### プロンプト変更履歴の保存・閲覧

- **新規行として追記する場合の内容**:
  - 媒介: `WEB`
  - 該当箇所: `プロンプト設定（/admin/prompt）`
  - 改善したい内容: `プロンプト変更履歴を残してチューニング作業を振り返れるようにしたい（会議要望）`
  - 改善案: `保存時に本文＋任意メモを履歴テーブルに記録、画面下に履歴一覧を表示`
  - 記入者: `朝香（会議発言）`
  - 実施ステータス: `完了`
  - 備考:
```
保存時に prompt_history テーブルに本文＋任意メモを自動記録。画面下部の履歴一覧から本文展開・エディタへ復元可能。辻村さんがチューニング時に「いつ・何を狙って変えたか」を後から振り返れる。
```

---

# デプロイ前に必要な作業（共有メモ）

1. Supabaseで以下のマイグレーションを適用:
   - `00008_add_feedback.sql`
   - `00009_add_prompt_history.sql`
2. Supabase Auth のメールテンプレートで「Reset Password」のリダイレクトURLを本番ドメインに設定
